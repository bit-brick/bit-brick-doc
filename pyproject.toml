[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "bit-brick"
authors = [{name = "bit-brick", email = "bit-brick@bit-brick.com"}]
dynamic = ["version", "description"]
